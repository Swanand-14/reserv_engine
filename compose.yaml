services:
  dbservice:
    image: mysql:latest
    container_name: reserveengine-dev-db

    ports:
      - "3306:3306"

    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: reserve_engine

    volumes:
      - /Users/swanand/Documents/Coding/java/reserv-engine/data:/var/lib/mysql